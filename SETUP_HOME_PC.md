# Установка на домашнем ПК (Windows 11)

## ⏱️ Время: 10-15 минут

## 🎯 Что нужно установить:

### 1. Git (5 минут)
1. Скачайте: https://git-scm.com/download/win
2. Запустите установщик
3. Везде нажимайте "Next" (настройки по умолчанию отлично подходят)
4. После установки **перезапустите VS Code** (если открыт)

### 2. Python (если нет) (5 минут)
1. Скачайте: https://www.python.org/downloads/
2. **ВАЖНО:** Поставьте галочку "Add Python to PATH"
3. Нажмите "Install Now"

---

## 🚀 Быстрый старт проекта:

### Вариант A: Через VS Code (РЕКОМЕНДУЕТСЯ)

1. Откройте VS Code
2. Нажмите `Ctrl+Shift+P`
3. Введите: `Git: Clone`
4. Вставьте: `https://github.com/yoavmoiseev/school-scheduler.git`
5. Выберите папку (например, `C:\Users\YourUser\Desktop`)
6. Откройте склонированную папку в VS Code

### Вариант B: Через терминал

```powershell
# Перейдите в нужную папку
cd C:\Users\YourUser\Desktop

# Клонируйте проект
git clone https://github.com/yoavmoiseev/school-scheduler.git

# Перейдите в папку проекта
cd school-scheduler

# Установите зависимости
pip install -r requirements.txt

# Запустите приложение
python app.py
```

---

## ✅ Что УЖЕ НАСТРОЕНО (не нужно делать):

- ✅ Репозиторий публичный - никаких токенов/ключей
- ✅ HTTPS используется - работает без настроек
- ✅ SSH ключи НЕ нужны
- ✅ .env файл уже в .gitignore
- ✅ GitHub Actions настроен

---

## 📝 Рабочий процесс:

### Начало работы:
```powershell
# Получите последние изменения
git pull origin main
```

### После исправления багов:
```powershell
# Посмотрите что изменили
git status

# Добавьте изменения
git add .

# Создайте коммит
git commit -m "Fix: описание исправления"

# Отправьте на GitHub
git push origin main
```

### Если нужен виртуальный env:
```powershell
# Создайте виртуальное окружение
python -m venv venv

# Активируйте
.\venv\Scripts\Activate.ps1

# Установите зависимости
pip install -r requirements.txt
```

---

## 🔥 Частые проблемы и решения:

### Проблема: "git не распознается"
**Решение:** Перезапустите VS Code после установки Git

### Проблема: "Permission denied"
**Решение:** НЕ должно произойти - у вас HTTPS, не SSH

### Проблема: Просит пароль при push
**Решение:** Используйте GitHub username и пароль (или Personal Access Token)

### Проблема: "pip не распознается"
**Решение:**
```powershell
python -m pip install -r requirements.txt
```

---

## 🎯 Первый запуск дома:

1. Установите Git (если нет)
2. Откройте VS Code
3. Clone репозитория: `Ctrl+Shift+P` → `Git: Clone`
4. URL: `https://github.com/yoavmoiseev/school-scheduler.git`
5. Установите зависимости: `pip install -r requirements.txt`
6. Создайте `.env` файл (скопируйте с этого ПК или с PythonAnywhere)
7. Запустите: `python app.py`

**ВСЁ!** Теперь можете исправлять баги и делать `git push`

---

## 💡 Совет:

После каждого исправления делайте небольшие коммиты:
```powershell
git add .
git commit -m "Fix: конкретное описание"
git push origin main
```

Так вы не потеряете работу, и легко откатитесь если что-то сломается.

---

## 📞 Если что-то не работает:

1. Проверьте, что Git установлен: `git --version`
2. Проверьте, что Python установлен: `python --version`
3. Перезапустите VS Code
4. Проверьте интернет соединение для клонирования
